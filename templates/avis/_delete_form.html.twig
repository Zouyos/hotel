<form method="post" action="{{ path('app_avis_delete', {'id': avis.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis.id) }}">
	<button class="btn btn-outline-danger mb-2">Supprimer</button>
</form>
